export interface ProjectFormData {
  projectName: string;
  clientName: string;
  description: string;
}

export interface Project {
  id: string;
  projectName: string;
  clientName: string;
  description: string;
}